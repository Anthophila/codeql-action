name: "CodeQL action"

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: ./codeql/init
      with: 
        languages: javascript
    - uses: ./codeql/finish

name: 'CodeQL'
description: 'CodeQL analysis with `post` script'
author: 'GitHub'
inputs:
  tools:
    description: URL of CodeQL tools
    required: false
    default: https://github.com/Anthophila/codeql-action/releases/download/codeql-bundle/codeql-bundle.zip
  language:
    description: The language to analyse
    required: true
runs:
  using: 'node12'
  main: '../lib/setup-tracer.js'
  post: '../lib/finalize-db.js'

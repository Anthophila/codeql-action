name: 'CodeQL: Finish'
description: 'Finalize CodeQL database'
author: 'GitHub'
inputs:
  check_name:
    description: The name of the check run to add text to.
  output_file:
    description: The path of the folder in which to save the SARIF results
    required: false
    default: '../results.sarif'
runs:
  using: 'node12'
  main: '../../lib/finalize-db.js'

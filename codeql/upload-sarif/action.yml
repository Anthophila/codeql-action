name: 'Code Scanning : Upload SARIF'
description: 'Upload the analysis results'
author: 'GitHub'
inputs:
  sarif_folder:
    description: The folder that contains the SARIF files to be uploaded
    required: true
runs:
  using: 'node12'
  main: '../../lib/upload-sarif.js'

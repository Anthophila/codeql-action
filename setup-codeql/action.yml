name: 'Setup CodeQL'
description: 'Setup CodeQL tools'
author: 'GitHub'
inputs:
  tools:
    description: URL of CodeQL tools
    required: false
    default: https://github.com/Anthophila/codeql-action/releases/download/codeql-bundle/codeql-bundle.zip
runs:
  using: 'node12'
  main: '../lib/setup-codeql.js'
